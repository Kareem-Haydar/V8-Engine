#include <Core/ECS.h>

// Implementation is in the header (header-only for templates)
// If you want to add non-template system logic, you can do it here.

namespace Core {
  // Example: You could add system registration and update logic here if needed.
}
